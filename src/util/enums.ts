export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
};

export enum BusinessType {
  FARM = 'FARM',
  EXTENSION = 'EXTENSION',
  SERVICE = 'SERVICE'
};

export enum BusinessStatus {
  INACTIVE = 'INACTIVE',
  ACTIVE = 'ACTIVE',
};